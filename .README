*** IF YOU INTEND TO USE THIS CODE IN A PUBLIC ENVIRONMENT, ***
*** CHANGE THE PASSWORD IN config/db.inc.php and mysql_setup.sql  *** 

This project is a simple exhibition of an JSON API->MySQL ETL, 
wrapped into a humble PHP MVC.

Data is fetched from the NHL's (as yet undocumented) open API. No
API key is required to access the NHL's data.

Comments and critique of this MVC and ETL are heartily welcomed at 
marcmondhaschen@[the ubiquitous google mail service].com